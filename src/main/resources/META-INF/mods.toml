modLoader = "javafml" #mandatory
loaderVersion = "${loader_version_range}" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.
license = "${mod_license}"

[[mods]] #mandatory
modId = "${mod_id}" #mandatory
version = "${mod_version}" #mandatory
displayName = "${mod_name}" #mandatory
authors = "${mod_authors}" #optional
description = '''${mod_description}'''

# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies."${mod_id}"]] #optional
modId = "forge" #mandatory
mandatory = true #mandatory
versionRange = "${forge_version_range}" #mandatory
ordering = "NONE"
side = "BOTH"# Here's another dependency

[[dependencies."${mod_id}"]]
modId = "minecraft"
mandatory = true
versionRange = "${minecraft_version_range}"
ordering = "NONE"
side = "BOTH"

#[[dependencies."${mod_id}"]]
#modId = "${template}"
#mandatory = true
#versionRange = "${template_version}"
#ordering = "AFTER"
#side = "BOTH"


# Lets Do
[[dependencies."${mod_id}"]]
modId = "${lets_do_farmcharm}"
mandatory = false
versionRange = "${lets_do_farmcharm_version}"
ordering = "AFTER"
side = "BOTH"

[[dependencies."${mod_id}"]]
modId = "${lets_do_bakery}"
mandatory = false
versionRange = "${lets_do_bakery_version}"
ordering = "AFTER"
side = "BOTH"

[[dependencies."${mod_id}"]]
modId = "${lets_do_brewery}"
mandatory = false
versionRange = "${lets_do_brewery_version}"
ordering = "AFTER"
side = "BOTH"

[[dependencies."${mod_id}"]]
modId = "${lets_do_candlelight}"
mandatory = false
versionRange = "${lets_do_candlelight_version}"
ordering = "AFTER"
side = "BOTH"

[[dependencies."${mod_id}"]]
modId = "${lets_do_vinery}"
mandatory = false
versionRange = "${lets_do_vinery_version}"
ordering = "AFTER"
side = "BOTH"

[[dependencies."${mod_id}"]]
modId = "${lets_do_herbalbrews}"
mandatory = false
versionRange = "${lets_do_herbalbrews_version}"
ordering = "AFTER"
side = "BOTH"

[[dependencies."${mod_id}"]]
modId = "${lets_do_meadow}"
mandatory = false
versionRange = "${lets_do_meadow_version}"
ordering = "AFTER"
side = "BOTH"

[[dependencies."${mod_id}"]]
modId = "${lets_do_wildernature}"
mandatory = false
versionRange = "${lets_do_wildernature_version}"
ordering = "AFTER"
side = "BOTH"

[[dependencies."${mod_id}"]]
modId = "${lets_do_beachparty}"
mandatory = false
versionRange = "${lets_do_beachparty_version}"
ordering = "AFTER"
side = "BOTH"

